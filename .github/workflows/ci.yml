name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --frozen

      - name: Byte-compile all modules
        run: uv run python -m compileall -q app core contracts scripts

      - name: Validate event contracts (schemas + templates + translations)
        env:
          REDIS_URL: redis://localhost:6379/0
        run: |
          uv run python -c "import app.engine.templates, contracts.events; from contracts.validator import validate_event_contracts; validate_event_contracts()"

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t notifty:ci .
